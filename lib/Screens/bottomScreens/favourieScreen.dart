import 'package:flutter/material.dart';

class FavourScreen extends StatelessWidget {
  const FavourScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Favour')),
    );
  }
}
